pre-commit:
  image: python:3.13
  stage: lint
  needs: []
  tags:
    - gcp
    - prod
  script:
    - curl -LsSf https://astral.sh/uv/install.sh | env UV_INSTALL_DIR="/bin" sh
    - uv sync
    - uv run pre-commit run --all-files
